# Server
PORT=3000
NODE_ENV=development

# Oracle DB — same as Spring Boot diagram_user
DB_USER=diagram_user
DB_PASSWORD=diagram_pass
DB_CONNECT_STRING=localhost:1521/XE

# JWT
JWT_SECRET=inventory_jwt_super_secret_2024_change_in_prod
JWT_EXPIRES_IN=8h

# CORS — allow Ionic dev server
CORS_ORIGIN=http://localhost:8100
